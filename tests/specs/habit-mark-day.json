[
  { "name": "Mark a day has a helpful empty state", "steps": [{ "send": { "callback": "habit:mark_day" }, "expect": [{ "method": "sendMessage", "payload": { "text": "No habits yet — tap Add habit to create one." } }] }] }
]
