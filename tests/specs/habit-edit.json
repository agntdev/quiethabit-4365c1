[
  { "name": "Edit has a warm empty state", "steps": [{ "send": { "callback": "habit:edit" }, "expect": [{ "method": "sendMessage", "payload": { "text": "No habits to edit yet — tap Add habit to create one." } }] }] }
]
