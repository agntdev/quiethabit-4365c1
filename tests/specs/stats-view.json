[
  { "name": "Stats has a helpful empty state", "steps": [{ "send": { "callback": "stats:view" }, "expect": [{ "method": "sendMessage", "payload": { "text": "No habits yet — tap Add habit to create one." } }] }] }
]
